import { LoadLocationDirective } from './load-location.directive';

describe('LoadLocationDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadLocationDirective();
    expect(directive).toBeTruthy();
  });
});
